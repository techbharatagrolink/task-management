# ============================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================
# Copy this file to .env and update with your actual values
# Command: cp env.template .env
#
# IMPORTANT: Never commit .env file to version control!
# This template file is safe to commit.

# ============================================
# Database Configuration
# ============================================
# Database connection settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=inhouse_management
DB_PORT=3306

# Connection pool settings
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0

# SSL/TLS settings (for production)
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true

# Database timezone
DB_TIMEZONE=Z

# Connection timeout (milliseconds)
DB_CONNECT_TIMEOUT=10000
DB_ENABLE_KEEP_ALIVE=true

# ============================================
# Application Configuration
# ============================================
# Environment: development, staging, production
NODE_ENV=development

# Application URL (used for CORS, redirects, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cookie Domain (optional - only set if you need cookies to work across subdomains)
# For most cases, leave this unset and the browser will use the current domain
# If you want cookies to work across subdomains (e.g., app.example.com and api.example.com),
# set this to your root domain with a leading dot: .example.com
# For custom domains on Vercel, typically leave this unset
# COOKIE_DOMAIN=.yourdomain.com

# Server port
PORT=3000

# ============================================
# JWT & Security Configuration
# ============================================
# JWT Secret Key - MUST be changed in production!
# Generate a secure random string (minimum 32 characters)
# You can generate one using: openssl rand -hex 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Session timeout (milliseconds) - 1 hour default
SESSION_TIMEOUT=3600000

# Inactivity timeout (milliseconds) - 30 minutes default
INACTIVITY_TIMEOUT=1800000

# ============================================
# Production Deployment Configuration
# ============================================
# Uncomment and update for production:
# NODE_ENV=production
# NEXT_PUBLIC_APP_URL=https://your-domain.com
# DB_SSL=true
# DB_HOST=your-production-db-host
# DB_USER=your-production-db-user
# DB_PASSWORD=your-secure-production-password

# ============================================
# Docker Configuration
# ============================================
# When using docker-compose.yml, use these settings:
# DB_HOST=mysql
# DB_USER=app_user
# DB_PASSWORD=secure_password
# DB_NAME=inhouse_management
# DB_PORT=3306

# ============================================
# GitHub Actions / CI/CD
# ============================================
# These should be set in GitHub Secrets (Settings → Secrets and variables → Actions):
# - DB_HOST
# - DB_USER
# - DB_PASSWORD
# - DB_NAME
# - JWT_SECRET
# - NEXT_PUBLIC_APP_URL
# - DEPLOY_HOST (for SSH deployment)
# - DEPLOY_USER (for SSH deployment)
# - DEPLOY_SSH_KEY (for SSH deployment)
# - DEPLOY_PORT (for SSH deployment, default: 22)
# - DEPLOY_PATH (application directory on server)

# ============================================
# Optional: Third-party Services
# ============================================
# Add any third-party service API keys here if needed:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_REGION=us-east-1

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file to .env: cp env.template .env
# 2. Update all values with your actual configuration
# 3. Never commit .env to version control
# 4. For production, use secure passwords and secrets
# 5. Rotate JWT_SECRET regularly for security

